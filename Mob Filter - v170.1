[Enable]
alloc(MobFilter1,256)
Label(Filter1)
Label(FilterMobs1)
Label(Exit1)
alloc(MobFilter2,256)
Label(FilterMobs2)
Label(Filter2)
Label(Exit2)
alloc(MobList,2048)

MobList:
dd #1120100 //Orange Mushroom
dd #2230100 //Evil Eye

00B9204B: //E8 ? ? ? ? 89 ? 24 ? 85 ? 0F 85 ? ? ? ? 50
jmp MobFilter1

MobFilter1:
call 00419670 //original op
push ebx
mov ebx,MobList

FilterMobs1:
cmp eax,[ebx]
je Filter1
cmp [ebx],00
je Exit1
add ebx,4
jmp FilterMobs1

Filter1:
pop ebx
jmp 00B921F7 //address below last CALL in function

Exit1:
pop ebx
jmp 00B92050 //Below MobFilter1 address

00B9227A: //E8 ? ? ? ? 8B ? 85 ? 74 ? 8D ? ? ? 00 00 B9 [2nd Result]
jmp MobFilter2

MobFilter2:
push ebx
mov ebx,MobList

FilterMobs2:
cmp eax,[ebx]
je Filter2
cmp [ebx],00
je Exit2
add ebx,4
jmp FilterMobs2

Filter2:
pop ebx
call 004C9530 //original op
mov esi,eax //original op
jmp 00B92556 //address below last CALL in function

Exit2:
pop ebx
call 004C9530 //original op
jmp 00B9227F //Below MobFilter2 address

[Disable]
00B9204B:
call 00419670

00B9227A:
call 004C9530

Dealloc(MobFilter1)
Dealloc(MobFilter2)
Dealloc(MobList)
